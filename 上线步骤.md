# 上线(你只需要做这一步)

项目已开发完成、本地全绿、并且已在真实 Cloudflare 上验证可部署。
只差把 `RULES_KV` 命名空间 id 和你的 API token 配置好,就能发布到你自己的账号。

## 复制下面三行,粘贴到终端执行

```bash
cd /Users/mango/project/cf-doh
export CLOUDFLARE_API_TOKEN=YOUR_TOKEN    # ← 替换成你自己的 token（勿把真实 token 提交进 git）
npx wrangler kv namespace create RULES_KV        # 复制输出的 id
```

把上一步输出的 `id` 填进 `wrangler.jsonc` 第 15 行(替换 `REPLACE_WITH_YOUR_KV_NAMESPACE_ID`),
然后:

```bash
./scripts/deploy.sh
```

脚本会自动:
1. 跑完整测试;
2. 重建单文件 bundle;
3. 若 KV id 仍是占位符,自动创建 KV 命名空间并回填;
4. `wrangler deploy` 发布到你账号。

## 之后
- 绑定你自己的域名(见 README「绑定域名并启用」);
- 客户端开启 DoH 填 `https://你的域名/doh`(POST 或 GET `?dns=`)即可。

## 需要你提供的唯一东西
你的 Cloudflare API Token(Workers + KV 写权限)。这是你的账号机密,助手无法也不应伪造。